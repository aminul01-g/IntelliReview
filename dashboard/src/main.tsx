import React from 'react'
import ReactDOM from 'react-dom/client'
import IntelliReviewDashboard from './intellireview_dashboard'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
        <IntelliReviewDashboard />
    </React.StrictMode>,
)
