[mutmut]
# Target files for mutation testing
paths = ["analyzer/feedback/severity_orchestrator.py", "analyzer/detectors/security_detector.py"]
# Use the project's pytest configuration
runner = "pytest"
# Fail if mutation score is below 80%
threshold = 80
